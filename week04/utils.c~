#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "utils.h"

char flipChar(char c)
{
  if('a' <= c && c <= 'z' )
    return c-'a' + 'z';
  else if('A' <= c && c <= 'Z')
    return c-'A' + 'Z';
  else if('0' <= c && c <= '9')
    return c-'0' + '9';

  return c;
}
